import Logo from "../Logo"

const Header = () => {
    return(
        <header className="px-40">
            <div>
                <Logo fontSize={30} />
            </div>
        </header>
    )
}

export default Header